#define GREEN "ff00ff00"
#define green GREEN
#define A B
#define B A
#define HIDE(_name) if (name:_name) { hidden = true; } // comment
#define HIGHLIGHT(_name, _color) if (name:_name) { color = _color; }

#define HIGHLIGHT2(_name, _color) \
if (name:_name) { \
  color = _color; \
  borderColor = _color; \
}

if (value:>10000000) {
    color = GREEN;
    textColor = green;
    showLootbeam = true;
}

HIDE("dragon crossbow")
HIGHLIGHT("zaryte crossbow", GREEN)
HIGHLIGHT2("zaryte pickaxe", GREEN)
A
